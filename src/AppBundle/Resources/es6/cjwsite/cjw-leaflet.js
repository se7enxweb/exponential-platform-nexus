import $ from 'jquery';
//import 'leaflet';
//import 'leaflet/dist/leaflet.css';

(() => {
  // Base initialization code

  // Create the `jac` container object, if not already done
  window.jac = window.jac || {};
  window.jac.leaflet = window.jac.leaflet || {};
})();

$(() => {
  // On ready
});
